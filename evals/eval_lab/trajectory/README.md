# Trajectory

Status: `NOT_RUN`; classification: `UNPROVEN` as a general trajectory suite.

The source-aware conversation adapter executes inside `core`, but its retained
cases are not a comprehensive state-transition oracle. EvalLab therefore does
not promote that coverage into a general trajectory pass.
