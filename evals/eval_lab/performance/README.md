# Performance

Status: `BLOCKED`; classification: `HISTORICAL`.

The existing round-two runner can create a worktree and its workload and host
environment are not completely bound. EvalLab v1 inventories it but refuses to
execute it. No historical timing is presented as current performance evidence.
