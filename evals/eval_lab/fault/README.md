# Fault

Status: `BLOCKED`; classification: `UNPROVEN`.

Fault assertions exist across unit tests, browser checks, hard-probe fixture
references, and FireLens-200 rows. They are not yet one identity-bound typed
matrix, and referenced cases are not treated as executed evidence.
