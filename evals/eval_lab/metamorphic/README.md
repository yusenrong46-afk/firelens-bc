# Metamorphic

Status: `NOT_RUN`; classification: `UNPROVEN` as a standalone suite.

ClaimBench v2 mutation cases execute inside `core`. The historical natural
language mutation runner has no separately frozen, current relation-pack
contract, so EvalLab does not claim a broader metamorphic pass.
