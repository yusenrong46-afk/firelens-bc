# RAG

Status: `BLOCKED`; classification: `UNPROVEN`.

The retrieval qualification runner is inventoried, but a current canonical
sealed retrieval-v3 dataset and joined retrieval-to-publication oracle are not
available. Provider execution is outside the zero-cost EvalLab policy.

`python -m firelens_eval run --suite rag` emits a truthful blocked envelope and
does not call a provider.
