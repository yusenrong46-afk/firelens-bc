# Judge

Status: `BLOCKED`; classification: `UNPROVEN`.

EvalLab v1 has no provider-backed judge suite. Failure records carry explicit
null judge metadata for deterministic oracles. Any future judge requires a
frozen rubric and prompt, application identity, bidirectional A/B order, an
OpenRouter cost ceiling and receipts, and a rule that factual correctness
cannot be overruled by preference voting.
