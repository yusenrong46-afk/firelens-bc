# UI

Status: `NOT_RUN`; classification: `KEEP_AS_ADAPTER`.

Fixture, full-stack, and deployed Reality browser commands are inventoried.
EvalLab v1 does not install browsers, start services, browse a deployment, or
incur provider cost. A future UI adapter must retain browser/runtime identity
and normalize actual assertions before it can report a result.
