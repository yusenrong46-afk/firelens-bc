# Assumption ledger

Every hypothesis inherited from the task brief, prior reports or documentation,
with the evidence that confirmed or overturned it. `INDEPENDENT_FINDING`
entries are conclusions that differ from a prior assumption.

| # | Prior assumption (source) | Evidence | Conclusion | Action |
| --- | --- | --- | --- | --- |
| 1 | P0-A: Kelowna wording fails because of a nonsense extracted place such as "city to them" (brief). | Local reproduction of 10 variants: "Where is the wildfire near **Kelowna**?" → province-wide fetch + "No fetched official record matched the requested fire name"; plural wording works. Trace: `live_named_fire.extracted_located_fire_name` captures "near Kelowna" as a fire name; `query_plan` then nulls the location because the "name" contains the place. The reject-list in `location_intent._REJECTED_PLACES` is a separate weakness (filler words as places). | **INDEPENDENT_FINDING**: the primary Kelowna failure is a named-fire/place collision inside the planner, not the geocoder and not the reject-list. The "city to them" symptom is the second, independent weakness (unvalidated place spans). | One understanding step extracts the place first and validates it; a named fire needs a name head, never a preposition phrase; no per-word reject-lists. |
| 2 | P0-D: RAG may have no source proof (brief). | API responses for the four guidance questions carry publisher, title, quote, URL, review state. The UI hides them under `<details>` → side panel → "Technical binding details". `supported_items` omits quote-only claims. | Retrieval and evidence are present; the failure is presentation. | Inline source proof; no RAG rewrite. |
| 3 | P0-D variant: grab-and-go answer is a generic "build a bag" claim (observed). | `resolve_capability` needs the exact string "wildfire grab-and-go bag"; without "wildfire" the request falls to hybrid retrieval where TC-FIRESMART-021-01 (page 4) displaces the page-5 checklist. | Exact string matching is being used as the *understanding* step. | Route contents questions by intent; bind sources second. |
| 4 | P0-E: a lazy chunk returned 404 for an unknown reason (brief). | Vercel build produces one lambda and no static outputs; all assets served by Python with `max-age=0` and CDN MISS; the `index-{hash}` compatibility route masks stale entry files but not lazy chunks; a tab holding the previous entry JS requests a chunk hash that the new lambda no longer contains. | Root cause: deployment replacement + no immutable caching + compatibility hack instead of a chunk-load recovery path. | Immutable hashed assets, delete the hack, manifest integrity check, `vite:preloadError` reload. |
| 5 | P0-C: the UI feels zoomed/jammed (brief). | Root font is 16 px; no `zoom`/`scale`. Desktop ≥1120 px uses a fixed-height grid with the answer in a ~240 px rail, nested scrollers, all-caps monospace chrome, a permanent disclaimer strip and a version pill. Document height equals viewport height; the answer overflows inside an inner scroller. | Not a font-size problem; an information-architecture problem (split shell + fixed viewport). | Rebuild the layout in document flow. |
| 6 | P0-B: no obvious Home (brief). | Brand link is `href="#top"`; reset only via a toolbar "Clear"/"New conversation" that lives inside the conversation column. | Confirmed. | Brand and a header "New search" both reset state. |
| 7 | FL200-108: "answers the second incident but leaves the ID unbound" (`remaining_for_astra.md`). | Reproduced sequence: after "Tell me more about that one" the visible list collapses to one record, so "What about the second one?" re-answers the *selected* record. | **INDEPENDENT_FINDING**: worse than documented — a wrong record, silently. | Ordinal frame = last list shown; unresolvable ordinal → explicit clarification. |
| 8 | Record identity is stable across turns (implied by the selected-record design). | `OBJECTID` changed for the same fire between two fetches 11 minutes apart (`dataLastEditDate` in between). | **INDEPENDENT_FINDING**: identity is unstable; stable official keys are ignored. | Use `FIRE_NUMBER` / `EMRG_OAA_SYSID`. |
| 9 | Hard probe reference 91/105 (brief, notes). | Offline rc2.2 profile → 91/105; historical profile → 82/105. Nine expectations were migrated by profile. | Number is real but profile-dependent. | Keep 91/105 floor under rc2.2; do not migrate more expectations. |
| 10 | ClaimBench 332/332 (brief). | Reproduced offline: 332 cases, unsafe false-accept rate 0.0. | Confirmed. | Must stay intact. |
| 11 | The immutable plan is the sole authority for geography (ADR 0017). | Provider cannot broaden geography (exact tool-argument matching); the planner itself broadens to province-wide when it loses the place. | Holds against the model, not against the planner's parsing. | Fix understanding; keep the tool-authorization check. |
| 12 | FL200-067 should include records + boundary (brief, gold). | `input_seatbelt` terminates before any fetch. Product Constitution §1 says safety-sensitive questions are still FireLens questions. | Agree with the brief; the current implementation contradicts its own constitution. | Two-clause representation. |
| 13 | FL200-073 needs a typed DriveBC handoff (gold). | Answer composes Vernon fires + generic "check road conditions" prose; `official_safety_links(include_road_conditions=True)` already exists. | Agree; small composition fix, not a new integration. | `OFFICIAL_HANDOFF` clause outcome with the DriveBC link. |
| 14 | FL200-074 collapses to one lane (notes). | Reproduced: high-risk redirect only. | Agree. | Count / history-unavailable / packing clause outcomes. |
| 15 | "Failure ≠ zero" holds (README). | `_map_layer_results` marks layers unavailable; compose forces deterministic "not an all-clear" text; verified by reading and by tests. | Confirmed. | Preserve unchanged; cover with the reality gate fixture journey. |
| 16 | The product is "answer-first" (architecture doc). | Live answers are semicolon lists with raw ISO timestamps, "priority sample" wording and "The model did not choose the record". | The backend is truthful but not written for a person. | Rewrite public live strings in plain language; keep the deterministic facts. |
| 17 | RAG retrieval quality might be weak (brief asks to check). | Four guidance questions all retrieve the correct governing documents; failures are in capability routing (#3) and presentation (#2). Baseline BM25+dense+RRF+rerank retained. | Retrieval is not the problem. | No retrieval changes. |
| 18 | Tests are authoritative (implied by 2017 passing tests). | Several tests pin exact `static_subrequest` strings, tool enum order, README phrases, and a 105-case roster; none exercise a natural question end-to-end in a browser against the real backend. | The suite is strong on safety/data contracts and weak on product truth. | Product Reality Gate; retire implementation-detail pins that block the redesign only where a stronger product test replaces them. |
