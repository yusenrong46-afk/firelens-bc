# Remaining issues

1. CSS source line count grew vs baseline; further deletion of unused historical selectors still desirable.
2. Baseline/after Playwright screenshots not captured locally (Chromium download blocked in sandbox).
3. Place/radius context chips and spatial headlines omit place name when API does not return a typed place label (`CoarseResolvedLocation` is lat/lng only).
4. `make v1-6-performance` not re-run after UI changes.
5. Focused real-provider suite (25–40 questions) not executed in this session (budget/time); rely on offline gates + Reality Gate after preview.
6. FL200-146/171/172 treated as polish REVIEW cases; no prompt rewrite without reproduced product defect.
