# Browser report

## Local
- Vitest + jsdom coverage for shell behaviors, readiness, live summary.
- Playwright browser binary install failed under sandbox network policy (`cdn.playwright.dev`).

## Required follow-up after preview
- Capture `screenshots/after|preview|production` at listed viewports.
- Run Product Reality Gate against preview and production URLs.
- Asset integrity: hard refresh, lazy map/charts 200, no module console errors.
