# FireLens polish independent read-only review

Review date: 2026-09-07. Reasoning level: High. Reviewer: independently delegated Codex agent; no implementation participation in this review.

## Verdict

**CODE_ACCEPTED_FOR_DOCUMENTATION_FINALIZATION.** No P0/P1, release-significant P2 code regression, source/publication-truth blocker, or invalid candidate identity was established in this bounded review. The frozen implementation can proceed to the separately requested documentation finalization.

**LIVE_PROVIDER_QUALIFICATION_BLOCKED; RELEASE_NO_GO.** The required mixed and pet real-provider journeys did not complete successfully. The eight-Ask allowance is exhausted. The final J01 retry recovered with the normal three-attempt policy; that recovery does not qualify the failed mixed/pet journeys. Offline, mock-browser, and deterministic-upstream built-stack passes are engineering evidence. They are not production verification, deployment approval, or a substitute for the incomplete real-provider matrix.

These verdicts apply to candidate `11613679c7910c0c15962ee16da49ba7bc530ff2`, tree `37a6cca440ddeae3687482fcb0999ec77d24e9bf`. Any documentation successor must identify its own commit/tree and explicitly carry forward the reviewed implementation identity. This report does not authorize push, merge, deployment, promotion, more Ask requests, or more spend.

## Identity and independence

- Checkout: `/Users/thomas/Documents/Codex/2026-09-06/firelens-polish`.
- Baseline: `fc9b72368456bfefa4b1251c41a62422939cc0ab`, tree `0be841458f4589b4a2919aaa57349e7569404334`, verified locally.
- Candidate HEAD/tree match the requested freeze. All **1,107/1,107** tracked files match `FINAL_FREEZE.json`, with no missing/unbound tracked paths and no mismatches. Git status was empty both before review and at sealing.
- Imported package: `/Users/thomas/Documents/Codex/2026-09-06/firelens-polish/src/firelens/__init__.py`.
- Current raw PreparedBC PDF independently hashes to `9185b43a8b9b57820c2fdd17a8e7887d483e02519358d001445460b4f44446d2`. The retained parent-run HTTP-200 check in `CURRENT_SOURCE_IDENTITY.json` reports the same canonical document hash.
- Read AGENTS.md and repository `firelens-release`, `firelens-ui-audit`, and `firelens-eval` skills. Applied the PDF skill for read-only rendering. No checkout file was edited, no secret file was read, no paid call was made, and no UI/API request was sent to port 8767.
- Source adoption, rc2.3 J01 migration, and the US$1 ceiling were treated as the existing owner decisions. None was reopened or manufactured.

`REVIEWER_CHECKS.json` records the verification summary, fresh command, and hashes of the retained artifacts inspected for this review. The report and reviewer renders reside outside the checkout.

## Findings requiring final handoff treatment

### Q1 — Required real-provider matrix remains incomplete (release gate)

`REAL_SMOKE.json` contains successful HTTP-200 nearby, selected-record follow-up, and grab-and-go guidance responses, followed by a J01 HTTP-503 rate-limit failure. `REAL_SMOKE_CONTINUATION.json` retains another J01 rate-limit response, a mixed packing/personal-evacuation request with HTTP 503/rate_limit, and a pet-packing request with HTTP 503/unavailable. The 320px pet screenshot visibly presents a recoverable unavailable state and does not manufacture guidance.

`REAL_SMOKE_FINAL_RETRY.json` records Ask 8 with `provider_max_attempts: 3`, HTTP 200, partial response, and an exact current-source quotation. The reviewer independently ran the current-source predicate against that actual response and it passed. The retained `REAL_J01_VALIDATION.json` additionally records zero generation attempts. Earlier failures remain failures; the successful retry is a separate result. The initial one-attempt harness and final normal-policy run must remain distinguished.

**Required disposition:** record the partial live success and failed mixed/pet journeys explicitly; keep release NO-GO until those mandatory journeys are successfully qualified under a separately available execution allowance. The retained errors do not by themselves establish a new code regression. No further calls were made by this reviewer.

### Q2 — Finalize checkpoint documentation with the actual completed/blocked gates

`docs/releases/firelens-polish.md`, the new system-card checkpoint, and the bounded-execution paragraph of `docs/reports/J01_CURRENT_ACCEPTANCE_PROPOSAL.md` still describe the earlier credential/index checkpoint. Their stale text is intentional pending this review, not evidence that the current credential is missing.

**Required documentation update after this report:** bind the reviewed SHA/tree; record the real 178-chunk index, 144 verified reuses and 34 new embedding inputs; record the current matrix and 97/105 rc2.3 result with floor 86; preserve J01 legacy FAIL/current PASS; include this independent code-acceptance verdict; replace the obsolete credential gate with the incomplete real-provider matrix and exhausted eight-Ask allowance. Preserve historical red records, distinguish settled spend from reservations, and keep production/deployment identity explicitly historical unless reverified. Update README wording only where necessary to separate the dated production state from the accepted local implementation. Do not turn final documentation into a release-GO claim.

### Q3 — Source click is verified; browser PDF rendering is not

The saved source-click row has the correct `source_link`, but `source_open_url` is an empty string. The retained harness catches popup load failures and does not assert the loaded document URL. The canonical source was independently fetched with HTTP 200 and the matching hash, but that is separate evidence.

**Required evidence wording:** say “canonical link/click verified and source bytes re-fetched”; do not claim successful browser PDF rendering from this artifact. This is an evidence limitation, not a demonstrated broken product link or additional code blocker.

## Reviewed behavior

### Source truth and publication

The adopted source revision is present in the corpus and typed inventory; retired-document rows are excluded from current evidence and the withdrawn `TC-GENERAL-036-01` is absent from the active inventory. The focused tests verify all sixteen adopted claim fields and current technical bindings, including the public document hash. Unaffected corpus lines are byte-identical to the historical snapshot.

The reviewer rendered and inspected the complete relevant PDF pages 5, 11, and 16. Page 5 visibly includes “Emergency plan and insurance/important papers”; page 11 separates the E/F gauge labels from the half-full/half-charged sentence; page 16 visibly continues “two to four weeks” into its symptom list. These checks support the three scoped extraction repairs and do not extend the owner decision to emergency-domain certification.

Public evidence now carries the document SHA through structured, quoted, grounded, and conflict responses. SourceProof groups by canonical URL plus revision and explains that the current publisher link can show a newer edition. Its excerpt comes from a cited support, while structured paraphrases retain their publication kind and the quote-only presentation explicitly labels exact source wording. The source hash and locator are disclosed on demand without becoming a second authority owner.

The pet inflection repair stays in existing request/support owners. Quote fallback invokes the existing exclusion-polarity check, preventing an inclusion checklist from answering a request to omit pet supplies. Adding typed action/object fields to relevance uses the existing typed record rather than adding another claim parser or generation path. Focused regression controls passed; no authority widening was identified.

### Partial, unavailable, freshness, and geometry

The summary distinguishes null layers from valid zero counts, names the unavailable layer, and labels retrieval time “Checked by FireLens.” Per-record UI uses “Source updated” for publisher time. One session timer updates age each minute and starts visible-session refreshes no more frequently than every five minutes; aborted/superseded responses are guarded.

The retained province-wide geometry observation contains four invalid alert geometries among forty features; it leaves that layer unavailable, while the bounded Kelowna observation has four valid features. This is a dated official-source observation. No invalid geometry is admitted, and there is no unauthorized quarantine/counting redesign.

Nonblocking presentation note: in `LiveDataStatus.tsx`, a combined partial-plus-stale snapshot takes the partial branch before the stale branch, so the compact status names missing coverage and check time but omits an explicit stale qualifier. It does not display a green/current label. A future bounded UI refinement can expose both attributes together; this observation is not a basis to reopen accepted spatial semantics or block this code candidate.

### UI, starters, and accessibility

The nearby starter now asks for official records rather than presupposing a single wildfire. The four starter paths have retained current-source deterministic fixture evidence. The reviewed real guidance screenshot visibly includes the revised checklist, a separate supporting passage, and a collapsed revision disclosure. The real nearby screenshot separates the answer, selectable records, publisher time, and partial coverage; it captures the map while loading, so it is not proof of completed real-map rendering.

The 320px unavailable screenshot is readable, with retry and official-map controls. Retained built-stack checks cover 1536/390/320 layouts, source disclosure, selection continuity, empty/unavailable states, keyboard behavior, and axe. The heading change places the current-question h1 in a labelled section; no loss of the question heading was identified.

The migrated historical interface selectors target the actual current surface. The removed lead-status assertion remains covered by the current R4 record-status assertion; distribution identity/status, explicit map and list selection, personal boundaries, and outage recovery remain checked in the current built-stack lane. Historical test snapshots and failed logs are preserved. The migrated suite's 51/51 result must not retrospectively relabel the original failed suite.

The visual hierarchy is adequate for a bounded engineering demo: answer first, inspectable source, records/selection when applicable, and a stable composer. This review does not establish broad human-usability performance or benchmark latency improvements. Optional community content and remaining source repetition do not constitute release blockers.

### Evaluator migration and dependency refactor

rc2.3 changes only J01 relative to rc2.2 and preserves the 105-case dataset and floor of 86. Current J01 requires a partial accepted response, exact bound current-source quotes, rationale or an explicit missing-explanation limitation, original question/history, and zero generation. The candidate validator recomputes and requires the same response's legacy failure.

All five response-semantic functions extracted from hard_probe_cli to hard_probe_semantics are AST-identical to baseline after ignoring the intentional argument-type annotation change. The refactor shares the existing predicates and avoids importing the execution harness for response-only checks. Current and historical contracts remain separately represented; it does not create a second publication authority.

The reviewer verified every archived-file hash against both its archive manifest and the corresponding baseline Git blob. Historical source binding tests now explicitly load the archived root. The current gas-condition control uses the surviving admitted gas-order claim because the old gas-leak claim was withdrawn. Its original test remains archived. Whitespace normalization preserves the rationale assertion's words. No frozen historical oracle was rewritten to make the new source pass.

The current hard-probe still retains eight failures: F06, F07, F09, I04, I08, K03, K09, and L05. The retained candidate validation reports no paired regressions. Historical core/public-v2/browser red dispositions remain historical; no broad legacy acceptance or production qualification follows from the current floor pass.

## Evidence executed versus inspected

Fresh reviewer execution: 79/79 focused source, pet, proof, J01, historical-binding, and quote-atomicity tests passed in 2.10 seconds with bytecode/cache writing disabled; exact tracked-file/hash and baseline checks; raw-PDF hash; full archive-versus-baseline byte checks; semantic-function AST comparison; current J01 package validation; and the current-source oracle over the final real J01 response. No full expensive suite or live-provider suite was rerun by the reviewer.

Inspected retained execution: make verify exit 0, with 2,623 backend tests and 709 subtests in each of its two backend invocations, 203 frontend tests, and 45 mock-browser passes/1 skip; 51/51 deterministic-upstream built-stack checks; 106/106 source-aware; 31/31 offline-fake ProductBench; structured publication structural pass and retained mutation results; 97/105 hard probe with current J01 true/legacy false and floor 86. Source-aware and ProductBench use explicit synthetic/offline provider artifacts, not the rebuilt real index as end-to-end provider proof.

The retained real index manifest binds a 178-by-1536 matrix (`d746759193fdbd844dfa173fca3898dba380ea2d3ad37da12d422643e2d4ce0d`) to corpus `4045d15f475c4987571dc190b2f67053cdd95690eeb02506b45cccb532d1578c`. Cache preflight compares complete rendered inputs and embedding configuration; the rebuild records 144 reuses and 34 new inputs in one provider embedding call. This is a real rebuilt index, distinct from the fake vectors used in offline fixtures.

## GMT story and next boundary

The five-minute GMT script has a credible engineering story: official records and selected identity, a revised checklist with inspectable support, deterministic ownership, and visible uncertainty. It is credible as a clearly labelled local engineering demonstration of the paths actually exercised. It cannot yet be represented as a fully qualified live release. Before a release/demo readiness claim, resolve the required mixed/pet provider gate and verify the exact target deployment; retain the source-rendering limitation unless that browser step is separately observed.

Final terminal verdict: **CODE_ACCEPTED_FOR_DOCUMENTATION_FINALIZATION / LIVE_PROVIDER_QUALIFICATION_BLOCKED / RELEASE_NO_GO**.
