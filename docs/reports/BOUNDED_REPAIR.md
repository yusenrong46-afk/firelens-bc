# Bounded repair after independent examination

This local candidate starts from frozen `46b631979f8f4f6b64d12f4e07f12c3a3e218e31`
(tree `6bca4d2b54677f70e3308f04b3030d917b417f12`) on
`codex/firelens-review-repair`. Sol and its evaluator bridge remain historical.
The independent examination returned the frozen candidate for bounded repair.
This builder context does not perform its own final independent examination.

Production changes remove the downstream location reinterpretation already
superseded by the server-built query plan and add bounded contact/document
capability matches. A validated guidance clause beside a declined safety clause
now keeps both the source wording and the existing safety boundary; partial
publication composition reuses the existing typed-section contract.
They reuse admitted conditional passages and publication
gates. No new approved safety claim or UI redesign is introduced. Removing the
unused location helper is a narrow ownership consolidation; broad simplification
remains unproven.

The frozen hard-probe runner, labels and historical browser file remain intact.
`python -m firelens_eval draft-public --output /absolute/new/results.json`
is a separate draft protocol. It consistently sends all 105 public cases to
`/api/v1/ask`, records frozen-rule findings separately, and checks explicit
fixture facts/completion independently of the production parser. Its fixed live
records come from the existing built-stack fixture; static retrieval uses the
admitted corpus with a fake provider. General semantic entailment is unmeasured.
Negated and conditional proposition controls prevent lexical occurrence alone
from becoming an asserted fact; ambiguous occurrences remain REVIEW.

The seven enum migrations and ten current browser interaction migrations require
contract-owner approval. L05 real-model completion, sealed retrieval, official
feeds, domain/human review, performance acceptance, preview and release remain
separate pending gates. Exposed cases are unsealed development data.

Required local handoff commands are `PYTHONPATH=src:tests make verify`, the draft
public-path comparison, and `make verify-candidate-browser`. The latter runs
`astra.spec.ts` and `review-v2.spec.ts` against a built frontend and real FastAPI
with synthetic upstreams; mocked browser tests remain in `make verify`.
Historical `fullstack.spec.ts` is separately retained and run. Lifecycle unit,
mock-browser, current built-stack and historical counts must remain separate.

All new command logs, raw responses, paired case records, approval proposals,
performance samples and exact identities are maintained outside Git in
`private retained evidence: firelens-review-repair-evidence/`.
`REVIEW_PACKET.md` there is the neutral final handoff, once frozen. Original
builder/reviewer manifests and files are preserved. Descriptive local timing is
not performance acceptance, provider latency, deployment capacity or release GO.

## Bounded completion of DISTANCE-001, I07, and J01

This follow-up starts from the accepted evaluator candidate `cc694ed814f862a2c3f10cfab11fcb17c6f1f825`.
The original independent records are retained in
`firelens-independent-reexamination-9acae8ed/NEW_FAILURE_RECORDS.jsonl`, with the
unchanged `independent_probes.py`, `extended_probes.py`, and `adjudication_probes.py`.
All three reproduce on that baseline. The accepted evaluator and original
regression evidence are preserved; see `docs/eval/K03_F10_EVALUATOR_REPAIR.md`
for acceptance scope and corrected unique-input accounting.

- **DISTANCE-001:** a personal distance request now requires an origin before
  dispatch. A generic selected-distance request preserves an existing derived
  distance; when no calculation exists, it requests an origin after lookup. Its resumable location prompt preserves the selected record and
  current question. Missing computation no longer asserts missing record geometry.
  Existing deterministic point/perimeter distance semantics remain unchanged.
- **I07:** a supported guidance clause survives an adjacent personal-decision
  refusal. Clause extraction restores question punctuation for exact catalogue
  lookup and reuses the existing typed guidance subject for other recognized
  guidance, preserving explicit source requirements. Grounded guidance can carry
  a separate safety boundary, with official handoffs prioritized within the
  existing four-link limit. No new source or publication authority is granted.
- **J01:** reviewed-tool dispatch preserves a significance follow-up as the current
  question; history resolves only its retrieval topic. Topic overlap alone cannot
  answer why. An explicit rationale in the same exact topic-matched candidate
  remains subject to the existing admission, safe-span, quote, and publication
  checks. Action-only typed claims cannot displace the rationale. When only the
  topic is supported, the missing explanation remains a visible limitation;
  material-limit selection cannot erase an explicit unsupported aspect.

Tests in `tests/test_product_completion.py` cover the original contracts and a
small predeclared set of neighboring positive, negative, and ambiguous cases.
Original reproductions and their responses, source/plan traces, case-level
comparisons, exact identities, and final verification are retained separately
in the bounded completion handoff. Counts distinguish named cases from unique
inputs and overlapping invocations. These are offline controlled-provider runs,
not measurements of real-provider quality. Historical failures, external quality
reviews, and release gates remain separate. No push, merge, or deployment is
part of this work.

### Autonomous backend completion: J01 review repair v1

The independent review of `6f73f32` identified two in-scope inherited defects:
an explicit current significance subject could be replaced by the earlier bag
topic, and a bare significance follow-up could lose a genuinely restricted user
antecedent. Current syntax now preserves explicit subject tokens as the rationale
constraint; only a bare reference inherits the prior user question's restriction.
The existing prohibition owner decides that restriction. Assistant text grants
no authority. When reviewed evidence lacks a topic-matched rationale, the static
service reports the evidence gap and the public loop keeps that abstention
terminal, without a discarded generation. Source/compiler gates are unchanged.

The single ten-case independent challenge batch and all intermediate failures
are retained under `firelens-product-completion-evidence-6f73f32/autonomous/`.
The old J01 results and frozen evaluation profiles remain intact. Independent
review 02 bound to `c42d446002d9f158859d14254f6b7d637a3cbbb0` verified every
delegated maintenance condition and permitted the following current-test update.


### Current contract `j01_preparedness_significance_current.v1`

| Current test | Former expectation | Version 1 expectation |
| --- | --- | --- |
| `test_high_risk_history_followup_does_not_consult_a_generating_planner` | Exact official handoff, no claims/evidence | Exact admitted PreparedBC bag rationale, accepted validation, quote/support/source identity, official-quote-only authority; zero planner, generation calls and generation stages |
| `test_rc2_run_preserves_legacy_j01_failure_and_v2_identity` (renamed) | Frozen RC2 J01 returned 0 and passed every invariant | Unchanged RC2 returns 1 and fails; same v2 identity and expectation hash; exact base-mode disagreement and false `zero_claims`, `zero_evidence`, `official_handoff` invariants; zero-generation/cost and reason-code protections still pass |

This permission is limited to the supported preparedness-significance case.
No-history, inventory-only, latest/wrong-topic, explicit-current-subject and
restricted-history controls remain active. Review 02 independently confirmed
all section 6 conditions in the retained goal, including existing source
admission/compiler permission and the adversarial generating-planner control.
The frozen RC2 effective expectation SHA256 remains
`9ba2751f5e81ce30fdb59616541a085c0de12dfab7ba2e273bc6c1d69a448e7f`.
No frozen profile, dataset, manifest or accepted nine-file evaluator is changed.
A passing current unit test about the legacy failure does not change that
profile's score. Comparisons must use identical evaluator/profile bytes.

The durable campaign record is
`private retained evidence: firelens-product-completion-evidence-6f73f32/autonomous/STATE.json`.
It binds the final SHA/tree, reviewer verdict, command exits, raw reports,
retained failures, skips and next external gates. The initial repaired freeze's
full run retained both old J01 failures plus two duplicate 350-line module guards;
the latter are corrected by moving the same terminal predicate into the existing
loop-support module, with the limit unchanged. A local variable rename resolves
the associated type-check collision. Neither changes source authority.

Real-provider generation and retrieval, sealed retrieval evaluation, current
official-feed qualification, performance acceptance, deployed/UI usability and
human/source/release review remain external work. Offline fixtures and local
browser journeys do not establish those results. The campaign stops after local
acceptance; no push, merge, deployment or provider spend is authorized.
